name: sign
description: Sign - on-device iOS IPA signing workspace.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_picker: ^8.1.7
  path: ^1.9.0
  path_provider: ^2.1.5
  shared_preferences: ^2.3.5

flutter:
  uses-material-design: true
